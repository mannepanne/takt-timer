node_modules
dist
dist-native
android
public/fonts
coverage
.wrangler
SPECIFICATIONS/prototype-design-files
migrations
pnpm-lock.yaml
# Claude-managed files — Prettier inflates blank lines around list items.
# A trailing comment would be read as part of the pattern (gitignore syntax
# only treats # as a comment at the start of a line), so keep it on its own.
.claude
